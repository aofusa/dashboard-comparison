BIND_ADDR=127.0.0.1:8080
JWT_SECRET=dev-insecure-change-me
# Access JWT の有効期限（分）。既定 15。
JWT_ACCESS_EXP_MINUTES=15
META_DB_PATH=meta.db
CONTENT_DB_PATH=content.duckdb
MOKA_MAX_BYTES=209715200
META_POOL_SIZE=8
# Refresh トークン有効期限（日）
JWT_REFRESH_EXP_DAYS=7
# tower-sessions（moka）最大セッション数
SESSION_MOKA_MAX=4096
# Arrow バイナリ応答（operationName ItemsArrowBinary）の最大バイト。0 = 無制限扱い。
ARROW_BINARY_MAX_BYTES=0
